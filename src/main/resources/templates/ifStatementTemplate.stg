import "util.stg"

ifStatementTemplate(expressionElement, statementElement, elseIfPartElement, elsePartElement) ::= <<
if(<expressionElement>) {
<list(statementElement)>
}
<list(elseIfPartElement)>
<elsePartElement>
>>